import LandingPage from "./pages/Landing/index";

// testing for now, its gonna be rendered in its own page
export default function Home() {
    return (
        <div className="font-[family-name:var(--font-geist-sans)] bg-[#E8EBED]">
            <LandingPage />
        </div>
    );
}
