export type Index = {
    c: number; // current price
    d: number; // change
    dp: number; // percentage change
    h: number; // high price of day
    l: number; // low price of day
    o: number; // open price of day
    pc: number; // prev close price
};
